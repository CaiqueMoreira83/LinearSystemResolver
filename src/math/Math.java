package math;

import bean.Equation;

/**
 * Class that hold static methods to handle all kinds of operations envolving linear systems and equations
 * @author caiquemoreira
 *
 */
public class Math {

    //public static Equation multiply(Equation equation, int value) {
    //    for (int i = 0; i < equation.getNumVariables(); i++)
           // equation.getVariables().get(i) *= 
    //}

}
